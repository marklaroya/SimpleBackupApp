name: Deploy on Dell Server

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy latest
        run: |
          set -e
          cd /home/server/prj/SimpleBackupApp
          git pull origin main
          docker compose up -d --build