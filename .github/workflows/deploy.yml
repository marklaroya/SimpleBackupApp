name: Deploy on Dell Server

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Deploy with Docker
        run: |
          docker compose down
          docker compose up -d --build